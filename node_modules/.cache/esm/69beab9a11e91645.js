let bcrypt;_11e‍.x([["comparePassword",()=>comparePassword]]);_11e‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);

async function comparePassword(password, hash) {
    const res = await bcrypt.compare(password, hash);
    return res;
}

